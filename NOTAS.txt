---install go/bin

wget https://golang.org/dl/go1.16.6.linux-amd64.tar.gz
tar -C /usr/local -xzf go1.16.6.linux-amd64.tar.gz
añadir rutas

https://github.com/iver/vagrant-golang
https://github.com/iver/vagrant-golang

golang.sh

cambiar debian por linux en url de descarga

[ -d ~/Config ] || git clone https://github.com/iver/Baubles.git ~/Config --branch linux;

cambiar version:

export GOPACK=go1.13.1.linux-386.tar.gz

sudo visudo

añadir rutas:

nano ~/.profile  
source ~/.profile 
 
/usr/local/go/bin
/home/vagrant/go/bin


sudo visudo

ruta de ejecuin

http://10.2.2.205:9000/


sudo find /home/vagrant/go/ -type d -exec chmod 755 {} \;
sudo find /usr/local/pgsql/ -type d -exec chmod 755 {} \;
sudo find /usr -type d -exec chmod 755 {} \;


sudo find /home/vagrant/go/bin/revel -type f -exec chmod 644 {} \;
sudo find /usr -type f -exec chmod 644 {} \;

--- REVEL

go get github.com/revel/revel
go get github.com/revel/cmd/revel
go get github.com/revel/modules/static
go get github.com/garyburd/redigo/redis
go get github.com/patrickmn/go-cache
go get github.com/bradfitz/gomemcache/memcache


revel new -a platzi/chat
revel run platzi/chat

depues de cargar todas las libs:
go mod tidy

http://10.2.2.205:9000/

*-------------- VAGRANT

vagrant up --provision
vagrant ssh

--postgres

configure with CFLAGS="-O1"
https://revel.github.io/tutorial/gettingstarted.html
https://www.thegeekstuff.com/2009/04/linux-postgresql-install-and-configure-from-source/

wget https://ftp.postgresql.org/pub/source/v13.3/postgresql-13.3.tar.gz
tar xvfz postgresql-13.3.tar.gz
cd postgresql-13.3

configure
make
make install
ls -l /usr/local/pgsql/

-- CREATE USER
adduser postgres
passwd postgres

--CHECK PERMISIONS
mkdir /usr/local/pgsql/data
chown postgres:postgres /usr/local/pgsql/data
ls -ld /usr/local/pgsql/data

--CREATE DATA DIR
su - postgres
/usr/local/pgsql/bin/initdb -D /usr/local/pgsql/data/
ls -l /usr/local/pgsql/data

-- CHECK LOG
/usr/local/pgsql/bin/postmaster -D /usr/local/pgsql/data >logfile 2>&1 &
cat logfile

-- CREATE DB
/usr/local/pgsql/bin/createdb test
/usr/local/pgsql/bin/psql test
 
sudo apt-get install build-essential
sudo apt-get install git build-essential libreadline-dev
sudo apt-get install zlib1g-dev

---goose

https://github.com/steinbacher/goose
